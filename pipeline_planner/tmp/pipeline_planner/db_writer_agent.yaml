name: db_writer_agent
agent_class: LlmAgent
description: Executes approved database write operations.
model: gemini-2.5-pro
instruction: 'Upon receiving admin approval, safely execute the database write operations
  as specified.

  Ensure data integrity and log the changes made.

  '
tools:
- name: pipeline_planner.tools.database_tools.db_writer
callbacks:
  before_tool_callbacks:
  - name: pipeline_planner.callbacks.admin_approval_callbacks.check_db_write_approval
